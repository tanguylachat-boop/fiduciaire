# AUDIT — Fiduciaire AI POC (2026-04-28)

État factuel du repo, lecture seule. Référence : `README.md`, `DECISIONS.md`, `docs/pipeline.md`, `docs/scope.md`, `worker/README.md`.

## 1. État vs planning README

| Jour | Livrable prévu | Statut |
|---|---|---|
| **Lun** | Repo + Ollama + classification 5 docs réels + précision | 🟡 Repo OK, Ollama OK (`qwen2.5:7b/14b`, `qwen2.5vl:7b` pullés), bench **latence** lancé (`DECISIONS.md:180-223`), mais **bench précision sur docs réels NON LANCÉ** — `data/samples/labels.csv:1-3` ne contient qu'un commentaire, **0 PDF** dans `data/samples/`. |
| **Mar** | Pipeline E2E (watcher → OCR → LLM → rename → move → SQLite) | ❌ `worker/src/fiduciaire_worker/__main__.py:5` = `NotImplementedError`. Aucun module pipeline. |
| **Mer** | Dashboard Next.js minimal (queue review, lecture seule) | 🟡 Dashboard existe mais c'est le scaffold **Phase 2/3** (mock-data, Supabase, sidebar SaaS). Pas de page `/review`. Pas de branche SQLite. |
| **Jeu** | Bench 50 docs + précision finale + rapport | ❌ Pas lancé, corpus inexistant. |
| **Ven** | Loom + pitch + propal 3 paliers + démo paramétrabilité | ❌ Aucun livrable. |

## 2. Worker — pipeline `docs/pipeline.md`

Fichiers Python implémentés : `__main__.py` (stub), `scripts/bench.py`, `scripts/latency_bench.py`, `scripts/quality_test.py`. Modules pipeline (préparation, qr-bill, ocr, classify, rename, route, db) **absents**.

| Étape | Statut |
|---|---|
| 1. Préparation (sha256 + archive + INSERT SQLite) | ❌ |
| 2. Pré-parser QR-bill (`pyzbar` + Swiss Payments Code) | ❌ — `pyzbar` listé dans `worker/pyproject.toml:11`, jamais utilisé. `bench.py:114-135` ignore explicitement (commentaire `qr_used=False — TODO Mardi`). |
| 3. OCR Tesseract fr+deu | 🟡 Logique OCR seule présente dans `bench.py:114-135`, pas de fallback vision, pas de calcul `ratio_text`. |
| 4. Classification Ollama JSON | 🟡 Présente dans `bench.py:144-158` et `quality_test.py:132-146`. Pas de retry sur JSON cassé. |
| 5. Renommage selon `naming.pattern` | ❌ |
| 6. Routing `data/clients/<slug>/<an>/<type>/` | ❌ |
| 5'. Review queue (seuils + `data/needs-review/`) | ❌ |
| Schéma SQLite (`docs/pipeline.md:80-115`) | ❌ Aucun `.sql`/`.py` ne le crée. |
| Tests | ❌ `worker/tests/` vide (`.gitkeep` seul). |

## 3. Dashboard Next.js

Components créés : `PageHeader`, `ActivityFeed`, `Sidebar`, `AccuracyChart`, `VolumeChart`, `ReminderTimeline`, `Kpi`, `Card`, `Badge`, `AgentLevelDot`. **Tous branchés sur `MOCK_DOCUMENTS/EXTRACTIONS/ACTIONS`** (`lib/mock-data.ts`, cabinet fictif "Morand & Associés", plan comptable). Pages : `/`, `/documents`, `/documents/[id]`, `/ecritures`, `/relances`, `/performance`, `/parametres`. **Pas de `/review`**. Sidebar (`components/Sidebar.tsx:18-25`) pointe sur les pages SaaS Phase 2/3. Connecteur DB : `lib/supabase/{client,server}.ts` — **pas SQLite**.

## 4. Bench GO/NO-GO (≥80 % type sur 20 docs)

- Lancé : ❌ Aucun PDF dans `data/samples/`, `labels.csv` non rempli.
- Score type / client / date / montant : **non mesuré**.
- Latence médiane : 12.66 s warm sur **1 cas factice Swisscom** (`DECISIONS.md:204`), Qwen 7B + prompt v2.
- Cas d'échec corpus réel : non identifiés (corpus inexistant).
- Test annexe `quality_test.py` : 3/3 sur cas factices matching client → **ne valide pas le critère 80 % `type`** (`DECISIONS.md:223`).

## 5. Phase 2/3 vs POC

Conflit fort. `app/`, `components/`, `lib/supabase/`, `lib/mock-data.ts`, `supabase/migrations/0001_init_fiduciaire.sql` (multi-tenant `ai_clients`), `n8n/01-04*.json` sont tous orientés SaaS Phase 2/3. `DECISIONS.md:20` annonce "mise en standby" mais rien n'est isolé. Mercredi imposera soit un gut total du dashboard, soit un sous-dossier `dashboard-poc/`.

## 6. Risques bloquants

1. **Corpus réel inexistant** → bloque Lundi ET Jeudi. Sans 20 docs anonymisés, aucun chiffre commercialisable.
2. **Pipeline Mardi non commencé** → 4 jours pour 6 étapes + SQLite + tests.
3. **Dashboard à refondre** → décider Mercredi matin : repartir minimal vs adapter.
4. **Pas de bench Mac Mini prod** → tous chiffres MBP 16 GB Qwen 7B, pas Llama 70B.
5. **Tentation com client** → citer le 3/3 factice viole `DECISIONS.md:174`.

## 7. Action immédiate

**Constituer le corpus de 20 docs réels anonymisés et remplir `data/samples/labels.csv`**, puis lancer `worker/scripts/bench.py`. C'est le verrou : aucun jalon Mar-Jeu-Ven n'est atteignable sans corpus, et l'absence de docs concrets fait porter tout le risque produit sur des cas factices. 1h de constitution corpus = débloque toute la semaine.
